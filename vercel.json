{
	"version": 2,
	"buildCommand": "bun run build",
	"outputDirectory": ".next",
	"framework": "nextjs",
	"git": {
		"deploymentEnabled": {
			"dev": true
		}
	},
	"env": {
		"NODE_ENV": "development"
	}
}
